import {prisma} from "@/lib/prisma"; import {KpiCard} from "@/components/KpiCard"; import {TrafficBadge} from "@/components/TrafficBadge";
export default async function HeadOperacoesPage(){const indicators=await prisma.indicator.findMany({where:{OR:[{area:"OPERACOES"},{level:"ESTRATEGICO"}]},include:{results:{orderBy:{referenceDate:"desc"},take:1}}});const critical=indicators.filter(i=>i.results[0]?.trafficLight==='VERMELHO');const actions=indicators.filter(i=>i.results[0]?.actionPlan);return <><section className="page-header"><div><h2>Dashboard do Head de Operações</h2><p>Gargalos, indicadores críticos, planos de ação e rotina semanal.</p></div></section><section className="grid grid-3"><KpiCard label="Indicadores monitorados" value={indicators.length}/><KpiCard label="Indicadores críticos" value={critical.length}/><KpiCard label="Planos de ação abertos" value={actions.length}/></section><section className="card" style={{marginTop:18}}><h3>Fila de gestão semanal</h3><table className="table"><thead><tr><th>Indicador</th><th>Atingimento</th><th>Semáforo</th><th>Ação recomendada</th></tr></thead><tbody>{indicators.map(i=><tr key={i.id}><td>{i.code} - {i.name}</td><td>{i.results[0]?.achievement.toFixed(1)??"-"}%</td><td>{i.results[0]?<TrafficBadge value={i.results[0].trafficLight}/>:"-"}</td><td>{i.results[0]?.actionPlan??"Acompanhar rotina"}</td></tr>)}</tbody></table></section></>}
