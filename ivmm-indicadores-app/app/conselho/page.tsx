import {prisma} from "@/lib/prisma"; import {KpiCard} from "@/components/KpiCard"; import {TrafficBadge} from "@/components/TrafficBadge";
export default async function ConselhoPage(){const indicators=await prisma.indicator.findMany({where:{level:"ESTRATEGICO"},include:{results:{orderBy:{referenceDate:"desc"},take:1}}});const avg=indicators.length?indicators.reduce((a,i)=>a+(i.results[0]?.achievement??0),0)/indicators.length:0;const risks=indicators.filter(i=>i.results[0]?.trafficLight!=="VERDE");return <><section className="page-header"><div><h2>Dashboard do Conselho Consultivo</h2><p>Visão estratégica e sintética para tomada de decisão.</p></div></section><section className="grid grid-3"><KpiCard label="Indicadores estratégicos" value={indicators.length}/><KpiCard label="Score estratégico" value={`${avg.toFixed(1)}%`}/><KpiCard label="Pontos de atenção" value={risks.length}/></section><section className="card" style={{marginTop:18}}><h3>Pauta sugerida</h3><table className="table"><thead><tr><th>Indicador</th><th>Atingimento</th><th>Status</th><th>Discussão recomendada</th></tr></thead><tbody>{indicators.map(i=><tr key={i.id}><td>{i.code} - {i.name}</td><td>{i.results[0]?.achievement.toFixed(1)??"-"}%</td><td>{i.results[0]?<TrafficBadge value={i.results[0].trafficLight}/>:"-"}</td><td>{i.results[0]?.trafficLight==='VERDE'?"Manutenção e escala":"Analisar causa, risco e plano de ação"}</td></tr>)}</tbody></table></section></>}
