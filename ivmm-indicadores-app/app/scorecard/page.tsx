import {prisma} from "@/lib/prisma"; import {formatArea} from "@/lib/kpi"; import {TrafficBadge} from "@/components/TrafficBadge"; const areas=["FINANCEIRO","COMERCIAL","RH","OPERACOES","MARKETING","ADMINISTRATIVO"];
export default async function ScorecardPage(){const indicators=await prisma.indicator.findMany({include:{results:{orderBy:{referenceDate:"desc"},take:1}}});const rows=areas.map(area=>{const items=indicators.filter(i=>i.area===area&&i.results[0]);const avg=items.length?items.reduce((a,i)=>a+i.results[0].achievement,0)/items.length:0;return{area,total:items.length,avg,status:avg>=100?"VERDE":avg>=80?"AMARELO":"VERMELHO"}});return <><section className="page-header"><div><h2>Scorecard Automático por Área</h2><p>Desempenho por Financeiro, Comercial, RH, Operações, Marketing e Administrativo.</p></div></section><section className="card"><table className="table"><thead><tr><th>Área</th><th>Indicadores ativos</th><th>Atingimento médio</th><th>Semáforo</th></tr></thead><tbody>{rows.map(r=><tr key={r.area}><td>{formatArea(r.area)}</td><td>{r.total}</td><td>{r.avg.toFixed(1)}%</td><td><TrafficBadge value={r.status}/></td></tr>)}</tbody></table></section></>}
